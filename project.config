[access]
	inheritFrom = Public-Plugins
[access "refs/*"]
	owner = group plugins-plugin-manager
	push = +force group plugins-plugin-manager
[project]
	description = One plugin to rule them all: install new plugins from Gerrit GUI
